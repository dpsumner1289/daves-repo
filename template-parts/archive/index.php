<?php
if (!function_exists('render_ct_archive')) :
    function render_ct_archive($content_type)
    {
        if ($content_type == 'video') :
        // render_video_archive();
        else :
        // render_archive();
        endif;
    }
endif;
